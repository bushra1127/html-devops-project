# HTML DevOps Project

Simple static website served with nginx. Jenkins pipeline builds a Docker image and deploys to Kubernetes.

### Setup
1. Replace placeholders in `Jenkinsfile`.
2. Create Docker Hub and kubeconfig credentials in Jenkins.
3. Create Jenkins pipeline (Multibranch or Pipeline from SCM).
4. Push to GitHub and trigger pipeline.

